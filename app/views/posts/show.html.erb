
<h2><%= @post.title %></h2>
<p><%= @post.content %></p>


<ul>
  <li><%= link_to "Edit", post_path(@post) %></li>
  <li><%= link_to "Destroy", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<p><%= link_to "Posts", posts_path %></p>
